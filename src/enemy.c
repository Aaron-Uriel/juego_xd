#include "world.h"

/*void enemy_decide(struct Entity *player, struct World *world) {
    struct TaggedCell (*cell_map)[world->width] = (struct TaggedCell(*)[world->width]) world->cells;
    

    for (uint8_t i = 0; )
}*/